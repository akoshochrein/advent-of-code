const input = `Sabqponm
abcryxxl
accszExk
acctuvwj
abdefghi`;

const lines = input.split("\n");

const mountainRaw = lines.map((l) => l.split(""));

for (let i = 0; i < mountainRaw.length; ++i) {
    for (let j = 0; j < mountainRaw[0].length; ++j) {
        if (mountainRaw[i][j] === "S") {
        }
    }
}

export {};
