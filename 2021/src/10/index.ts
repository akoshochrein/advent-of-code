import { readFileSync } from "fs";

const lines = readFileSync("./src/10/input.txt", "utf-8").split("\n");

for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    const bracketStack = [];
}
